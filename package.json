{
  "name": "new-in-5",
  "version": "1.0.0",
  "private": true,
  "description": "New in 5 mobile app",
  "scripts": {
    "prepare:web": "rm -rf www && mkdir -p www && cp index.html www/index.html",
    "ios:sync": "npm run prepare:web && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "8.4.2",
    "@capacitor/ios": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2"
  }
}
